//========================================================================
// GLFW 3.3 - www.glfw.org
//------------------------------------------------------------------------
// This is the generated glfw_config.h for the Win32 static build.
//
// GLFW normally produces this file through CMake; for a direct build we
// define it manually. The version macros come from include/GLFW/glfw3.h,
// so only the platform selection is needed here.
//========================================================================

// Build GLFW for Win32
#define _GLFW_WIN32 1
